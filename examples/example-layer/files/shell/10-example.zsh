# Example shell snippet (layer: example) — deployed to ~/.zsh and sourced by the
# layer-provided ~/.zshrc. Copied verbatim (not templated): write plain zsh and
# self-guard on tool presence. Filenames sort within a layer, so prefix with a
# number to control order (e.g. 10-path.zsh before 20-aliases.zsh).
alias ll='ls -lah'
