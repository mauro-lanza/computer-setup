name: example
# The layer schema version this layer targets. The orchestrator refuses to
# load a layer whose schema_version exceeds what it supports.
schema_version: 1
description: >-
  Reference content layer. Copy this directory into a new git repo to create
  your own layer. Delete the parts you don't need — every file except layer.yml
  is optional.
