---
manager:
  name: homebrew
  tier: generic
  summary: "Install Homebrew formulae and casks (baseline + selected; adopt allow-list)."
  capabilities: []
  consumes:
    - homebrew_baseline_formulae
    - homebrew_baseline_casks
    - homebrew_adopt_casks
    - homebrew_optional_formulae
    - homebrew_optional_casks
